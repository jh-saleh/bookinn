import { HostInformationIconPipe } from './host-information-icon.pipe';

describe('HostInformationIconPipe', () => {
  it('create an instance', () => {
    const pipe = new HostInformationIconPipe();
    expect(pipe).toBeTruthy();
  });
});
