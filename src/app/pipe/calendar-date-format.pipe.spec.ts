import { CalendarDateFormatPipe } from './calendar-date-format.pipe';

describe('CalendarDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
